(load "Sect01_24_even_odd.scm")
(load "Sect01_31_sum.scm")
(define (simpson-integral f a b n)
  (define (term x)
    (cond ((or (= x a) (= x b)) (f x))
          ((odd? (/ (- x a) (/ (- b a) n))) (* 4 (f x)))
          (else (* 2 (f x)))))
  (define (next x)
    (+ x (/ (- b a) n)))
  (* (sum term a next b)
     (/ (- b a) 3.0 n)))
